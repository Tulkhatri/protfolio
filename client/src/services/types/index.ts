export interface UserType {
  email: string;
  password: string | number;
}
export interface SkillType {
  title: string;
  description: string;
  edit_id: string | null;
}
export interface SkillId {
  id: number;
}
